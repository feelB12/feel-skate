{% extends "admin/base.html.twig" %}

{% block metatitle %}
    Créer une User| Skateboard Social Club
{% endblock metatitle %}

{% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('css/hom.css') }}">
        <link rel="stylesheet" href="{{ asset('css/search.css') }}">
{% endblock stylesheets %}

{% block main %}
    <section class="section">
        <div class="detail">
            <div class="justify-between flex fdr">
                <div class="flex">
                    {{ form(userForm)}}
                    {{ form_start(userForm) }}

                    {{ form_end(userForm) }}
                </div>
            </div>
        </div>
    </section>
{% endblock main %}
